# AgentPay Landing – Environment Variables
# Copy this file to .env.local and fill in the values.

# URL for the merchant dashboard (signup/login).
# Default in development: http://localhost:3000
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3000

# URL for the SDK / agent documentation.
# Set to an external URL (e.g. https://docs.agentpay.ai/sdk) or leave blank to use the built-in /docs page.
NEXT_PUBLIC_SDK_DOCS_URL=/docs

# Canonical site URL used for SEO metadata (og:url, etc.)
NEXT_PUBLIC_SITE_URL=https://agentpay.ai
